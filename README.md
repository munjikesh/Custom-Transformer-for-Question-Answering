# Custom-Transformer-for-Question-Answering
An end-to-end machine reading comprehension pipeline built for NLP (AID 829). Rather than fine-tuning an off-the-shelf pretrained model, we pretrained a BERT-style encoder entirely from scratch via masked language modeling, then branched it into two structurally distinct QA heads
